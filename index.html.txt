<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Relatório de Mercado — Consórcios 2025</title>
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.min.js"></script>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700;900&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>
  :root {
    --red: #c0392b;
    --red-dark: #a93226;
    --red-light: #e74c3c;
    --red-pale: #fdf0ef;
    --gray-dark: #2c2c2c;
    --gray-mid: #555555;
    --gray-light: #888888;
    --gray-bg: #f5f5f5;
    --gray-border: #e2e2e2;
    --white: #ffffff;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    font-family: 'DM Sans', sans-serif;
    background: var(--white);
    color: var(--gray-dark);
    min-height: 100vh;
    overflow-x: hidden;
  }

  /* HEADER */
  .header {
    background: var(--red);
    padding: 48px 60px 44px;
    position: relative;
    overflow: hidden;
  }

  .header::before {
    content: '';
    position: absolute;
    top: -80px; right: -80px;
    width: 380px; height: 380px;
    border-radius: 50%;
    background: rgba(255,255,255,0.05);
    pointer-events: none;
  }

  .header::after {
    content: '';
    position: absolute;
    bottom: -60px; left: 40%;
    width: 200px; height: 200px;
    border-radius: 50%;
    background: rgba(0,0,0,0.07);
    pointer-events: none;
  }

  .badge {
    display: inline-block;
    background: rgba(255,255,255,0.15);
    border: 1px solid rgba(255,255,255,0.3);
    color: #fff;
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 2.5px;
    text-transform: uppercase;
    padding: 5px 13px;
    border-radius: 2px;
    margin-bottom: 18px;
  }

  .header h1 {
    font-family: 'Playfair Display', serif;
    font-size: clamp(28px, 4vw, 48px);
    font-weight: 900;
    color: #fff;
    line-height: 1.1;
    margin-bottom: 10px;
  }

  .header h1 span { color: rgba(255,255,255,0.68); font-style: italic; }

  .header-sub {
    font-size: 12px;
    color: rgba(255,255,255,0.58);
  }

  .header-meta {
    position: absolute;
    top: 44px; right: 60px;
    text-align: right;
    z-index: 2;
  }

  .header-meta .prep-label {
    font-size: 10px;
    color: rgba(255,255,255,0.5);
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 5px;
  }

  .header-meta .company {
    font-family: 'Playfair Display', serif;
    font-size: 17px;
    font-weight: 700;
    color: #fff;
    letter-spacing: 0.3px;
  }

  .header-meta .author {
    font-size: 12px;
    color: rgba(255,255,255,0.62);
    font-style: italic;
    margin-top: 3px;
  }

  .header-meta .date {
    font-size: 11px;
    color: rgba(255,255,255,0.42);
    margin-top: 6px;
  }

  .for-pill {
    display: inline-block;
    background: rgba(255,255,255,0.12);
    border: 1px solid rgba(255,255,255,0.22);
    color: rgba(255,255,255,0.68);
    font-size: 10px;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    padding: 3px 9px;
    border-radius: 2px;
    margin-top: 8px;
  }

  /* LAYOUT */
  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 48px 60px;
  }

  .section-label {
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--red);
    margin-bottom: 6px;
  }

  .section-title {
    font-family: 'Playfair Display', serif;
    font-size: 24px;
    font-weight: 700;
    color: var(--gray-dark);
    margin-bottom: 26px;
  }

  .divider {
    height: 1px;
    background: var(--gray-border);
    margin-bottom: 46px;
  }

  /* RECORD BANNER */
  .record-banner {
    background: var(--red-pale);
    border-left: 4px solid var(--red);
    border-radius: 2px;
    padding: 18px 26px;
    margin-bottom: 46px;
    display: flex;
    align-items: flex-start;
    gap: 16px;
  }

  .record-icon { font-size: 28px; margin-top: 2px; }

  .record-text strong {
    display: block;
    font-size: 14px;
    font-weight: 600;
    color: var(--red-dark);
    margin-bottom: 4px;
  }

  .record-text span {
    font-size: 13px;
    color: var(--gray-mid);
    line-height: 1.6;
  }

  /* KPI CARDS */
  .kpi-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    margin-bottom: 48px;
  }

  .kpi-card {
    background: var(--white);
    border: 1px solid var(--gray-border);
    border-top: 3px solid var(--red);
    border-radius: 2px;
    padding: 22px 20px;
    transition: box-shadow 0.2s;
  }

  .kpi-card:hover { box-shadow: 0 4px 20px rgba(192,57,43,0.1); }

  .kpi-icon { font-size: 18px; margin-bottom: 12px; }

  .kpi-value {
    font-family: 'Playfair Display', serif;
    font-size: 28px;
    font-weight: 700;
    color: var(--gray-dark);
    line-height: 1;
    margin-bottom: 5px;
  }

  .kpi-label {
    font-size: 12px;
    color: var(--gray-light);
    margin-bottom: 12px;
    line-height: 1.4;
  }

  .kpi-badge {
    display: inline-flex;
    align-items: center;
    gap: 3px;
    font-size: 11px;
    font-weight: 600;
    padding: 3px 9px;
    border-radius: 2px;
    background: rgba(192,57,43,0.08);
    color: var(--red);
  }

  /* CHART CARDS */
  .charts-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 18px;
    margin-bottom: 48px;
  }

  .chart-card {
    background: var(--white);
    border: 1px solid var(--gray-border);
    border-radius: 2px;
    padding: 26px 26px 22px;
  }

  .chart-card.full { grid-column: span 2; }

  .chart-title {
    font-family: 'Playfair Display', serif;
    font-size: 15px;
    font-weight: 700;
    color: var(--gray-dark);
    margin-bottom: 3px;
  }

  .chart-sub {
    font-size: 11px;
    color: var(--gray-light);
    margin-bottom: 18px;
  }

  .chart-wrap { position: relative; height: 260px; }
  .chart-wrap.tall { height: 320px; }

  /* SEGMENT CARDS */
  .seg-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
    margin-bottom: 48px;
  }

  .seg-card {
    background: var(--white);
    border: 1px solid var(--gray-border);
    border-radius: 2px;
    padding: 20px;
  }

  .seg-name {
    font-size: 11px;
    font-weight: 600;
    color: var(--gray-dark);
    text-transform: uppercase;
    letter-spacing: 0.6px;
    margin-bottom: 14px;
    padding-bottom: 10px;
    border-bottom: 1px solid var(--gray-border);
  }

  .seg-metric {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 8px;
  }

  .seg-metric-label { font-size: 12px; color: var(--gray-light); }
  .seg-metric-value { font-size: 13px; font-weight: 600; color: var(--gray-dark); }

  .seg-growth {
    font-family: 'Playfair Display', serif;
    font-size: 20px;
    font-weight: 700;
    margin-bottom: 3px;
  }

  .seg-growth.positive { color: var(--red); }
  .seg-growth.negative { color: var(--gray-light); }

  .progress-bar {
    width: 100%;
    height: 3px;
    background: var(--gray-border);
    border-radius: 2px;
    margin-top: 12px;
    overflow: hidden;
  }

  .progress-fill {
    height: 100%;
    border-radius: 2px;
    background: var(--red);
  }

  .progress-fill.muted { background: #cccccc; }

  /* OUTLOOK */
  .outlook-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 18px;
    margin-bottom: 48px;
  }

  .outlook-card {
    background: var(--gray-bg);
    border: 1px solid var(--gray-border);
    border-left: 4px solid var(--red);
    border-radius: 2px;
    padding: 28px;
  }

  .outlook-number {
    font-family: 'Playfair Display', serif;
    font-size: 50px;
    font-weight: 900;
    color: var(--red);
    line-height: 1;
    margin-bottom: 4px;
  }

  .outlook-label {
    font-size: 12px;
    color: var(--gray-light);
    margin-bottom: 10px;
  }

  .outlook-desc {
    font-size: 13px;
    color: var(--gray-mid);
    line-height: 1.65;
  }

  /* FOOTER */
  .footer {
    background: var(--gray-dark);
    padding: 26px 60px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 16px;
  }

  .footer-left {
    font-size: 11px;
    color: #999;
    line-height: 1.9;
  }

  .footer-right { text-align: right; }

  .footer-right .for-label {
    font-size: 10px;
    color: #777;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 4px;
  }

  .footer-right .for-co {
    font-family: 'Playfair Display', serif;
    font-size: 20px;
    color: var(--red);
    font-weight: 700;
  }

  /* ANIMATIONS */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(18px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  .kpi-card  { animation: fadeUp 0.4s ease both; }
  .kpi-card:nth-child(1) { animation-delay: 0.05s; }
  .kpi-card:nth-child(2) { animation-delay: 0.10s; }
  .kpi-card:nth-child(3) { animation-delay: 0.15s; }
  .kpi-card:nth-child(4) { animation-delay: 0.20s; }
  .chart-card { animation: fadeUp 0.5s ease both; animation-delay: 0.25s; }
  .seg-card   { animation: fadeUp 0.5s ease both; animation-delay: 0.30s; }
</style>
</head>
<body>

<!-- HEADER -->
<div class="header">
  <div class="header-meta">
    <div class="prep-label">Elaborado por</div>
    <div class="company">Grupo Ágape Londrina</div>
    <div class="author">Gabriel Francisco</div>
    <div class="date">Fevereiro de 2026</div>
    <div class="for-pill">Preparado para Ademicon</div>
  </div>

  <div class="badge">Sumário Executivo · Mercado de Consórcios</div>
  <h1>Sistema de Consórcios<br><span>Fechamento 2025</span></h1>
  <p class="header-sub">Fonte: ABAC — Boletim do Sistema de Consórcios, Janeiro de 2026 (dados de Dezembro 2025)</p>
</div>

<!-- BODY -->
<div class="container">

  <!-- RECORD BANNER -->
  <div class="record-banner">
    <div class="record-icon">🏆</div>
    <div class="record-text">
      <strong>Ano histórico para o setor de consórcios</strong>
      <span>2025 bateu recordes consecutivos em adesões, participantes ativos e volume financeiro — superando mais do dobro da previsão de crescimento de 6% feita pela ABAC no início do ano. Pela primeira vez na história do setor, o Brasil ultrapassou 5 milhões de novas adesões e R$ 500 bilhões em créditos comercializados em um único ano.</span>
    </div>
  </div>

  <!-- KPIs -->
  <div class="section-label">Indicadores Principais · Acumulado 2025</div>
  <div class="kpi-grid">
    <div class="kpi-card">
      <div class="kpi-icon">📋</div>
      <div class="kpi-value">5,16M</div>
      <div class="kpi-label">Cotas vendidas em 2025</div>
      <span class="kpi-badge">▲ +15,0% vs 2024</span>
    </div>
    <div class="kpi-card">
      <div class="kpi-icon">💰</div>
      <div class="kpi-value">R$ 500Bi</div>
      <div class="kpi-label">Créditos comercializados</div>
      <span class="kpi-badge">▲ +32,1% vs 2024</span>
    </div>
    <div class="kpi-card">
      <div class="kpi-icon">👥</div>
      <div class="kpi-value">12,76M</div>
      <div class="kpi-label">Participantes ativos (dez/25)</div>
      <span class="kpi-badge">▲ +13,8% vs 2024</span>
    </div>
    <div class="kpi-card">
      <div class="kpi-icon">🎯</div>
      <div class="kpi-value">R$ 86,7K</div>
      <div class="kpi-label">Tíquete médio (dez/25)</div>
      <span class="kpi-badge">▲ +10,7% vs dez/24</span>
    </div>
  </div>

  <div class="divider"></div>

  <!-- CHARTS -->
  <div class="section-label">Evolução Histórica</div>
  <p class="section-title">Crescimento do Setor 2021–2025</p>

  <div class="charts-grid">
    <div class="chart-card">
      <div class="chart-title">Adesões Anuais (Milhões de Cotas)</div>
      <div class="chart-sub">Histórico 2021–2025 — recorde em 2025 com 5,16 milhões</div>
      <div class="chart-wrap"><canvas id="adesoes"></canvas></div>
    </div>

    <div class="chart-card">
      <div class="chart-title">Volume Financeiro Comercializado (R$ Bi)</div>
      <div class="chart-sub">Alta de 32,1% — primeiro ano a superar R$ 500 bilhões</div>
      <div class="chart-wrap"><canvas id="volume"></canvas></div>
    </div>

    <div class="chart-card full">
      <div class="chart-title">Crescimento por Segmento em 2025 (%)</div>
      <div class="chart-sub">Variação em adesões versus créditos comercializados — Eletroeletrônicos se destaca com +141% em créditos</div>
      <div class="chart-wrap tall"><canvas id="segmentos"></canvas></div>
    </div>

    <div class="chart-card">
      <div class="chart-title">Participantes Ativos (Milhões)</div>
      <div class="chart-sub">Base ativa superou 12 milhões pela primeira vez em julho/2025</div>
      <div class="chart-wrap"><canvas id="participantes"></canvas></div>
    </div>

    <div class="chart-card">
      <div class="chart-title">Projeção de Crescimento 2026 por Segmento (%)</div>
      <div class="chart-sub">Estimativas ABAC — imóveis lidera com +25% projetado</div>
      <div class="chart-wrap"><canvas id="projecao"></canvas></div>
    </div>
  </div>

  <div class="divider"></div>

  <!-- SEGMENTS -->
  <div class="section-label">Análise Setorial</div>
  <p class="section-title">Performance por Segmento em 2025</p>

  <div class="seg-grid">
    <div class="seg-card">
      <div class="seg-name">🏠 Imóveis</div>
      <div class="seg-metric">
        <span class="seg-metric-label">Crescimento adesões</span>
        <span class="seg-growth positive">+36,0%</span>
      </div>
      <div class="seg-metric">
        <span class="seg-metric-label">Crescimento créditos</span>
        <span class="seg-metric-value">+48,4%</span>
      </div>
      <div class="seg-metric">
        <span class="seg-metric-label">Relevância</span>
        <span class="seg-metric-value">Muito alta</span>
      </div>
      <div class="progress-bar"><div class="progress-fill" style="width:90%"></div></div>
    </div>

    <div class="seg-card">
      <div class="seg-name">🚗 Veículos Leves</div>
      <div class="seg-metric">
        <span class="seg-metric-label">Participantes ativos</span>
        <span class="seg-growth positive">5,38M</span>
      </div>
      <div class="seg-metric">
        <span class="seg-metric-label">Créditos movimentados</span>
        <span class="seg-metric-value">R$ 132,86Bi</span>
      </div>
      <div class="seg-metric">
        <span class="seg-metric-label">Share mercado interno</span>
        <span class="seg-metric-value">~30%</span>
      </div>
      <div class="progress-bar"><div class="progress-fill" style="width:73%"></div></div>
    </div>

    <div class="seg-card">
      <div class="seg-name">🏍️ Motocicletas</div>
      <div class="seg-metric">
        <span class="seg-metric-label">Adesões em 2025</span>
        <span class="seg-growth positive">1,44M</span>
      </div>
      <div class="seg-metric">
        <span class="seg-metric-label">Crescimento adesões</span>
        <span class="seg-metric-value">+8,3%</span>
      </div>
      <div class="seg-metric">
        <span class="seg-metric-label">Share mercado interno</span>
        <span class="seg-metric-value">30,8%</span>
      </div>
      <div class="progress-bar"><div class="progress-fill" style="width:52%"></div></div>
    </div>

    <div class="seg-card">
      <div class="seg-name">📱 Eletroeletrônicos</div>
      <div class="seg-metric">
        <span class="seg-metric-label">Crescimento adesões</span>
        <span class="seg-growth positive">+51,0%</span>
      </div>
      <div class="seg-metric">
        <span class="seg-metric-label">Crescimento créditos</span>
        <span class="seg-metric-value">+141,3%</span>
      </div>
      <div class="seg-metric">
        <span class="seg-metric-label">Destaque</span>
        <span class="seg-metric-value">Maior alta do setor</span>
      </div>
      <div class="progress-bar"><div class="progress-fill" style="width:96%"></div></div>
    </div>

    <div class="seg-card">
      <div class="seg-name">🛎️ Serviços</div>
      <div class="seg-metric">
        <span class="seg-metric-label">Créditos comercializados</span>
        <span class="seg-growth positive">R$ 1,13Bi</span>
      </div>
      <div class="seg-metric">
        <span class="seg-metric-label">Crescimento</span>
        <span class="seg-metric-value">+26,1%</span>
      </div>
      <div class="seg-metric">
        <span class="seg-metric-label">Drivers</span>
        <span class="seg-metric-value">Saúde · Turismo · Edu.</span>
      </div>
      <div class="progress-bar"><div class="progress-fill" style="width:44%"></div></div>
    </div>

    <div class="seg-card">
      <div class="seg-name">🚛 Veículos Pesados</div>
      <div class="seg-metric">
        <span class="seg-metric-label">Variação em adesões</span>
        <span class="seg-growth negative">Retração</span>
      </div>
      <div class="seg-metric">
        <span class="seg-metric-label">Causa principal</span>
        <span class="seg-metric-value">Queda de caminhões</span>
      </div>
      <div class="seg-metric">
        <span class="seg-metric-label">Outlook 2026</span>
        <span class="seg-metric-value">Estabilidade</span>
      </div>
      <div class="progress-bar"><div class="progress-fill muted" style="width:18%"></div></div>
    </div>
  </div>

  <div class="divider"></div>

  <!-- OUTLOOK -->
  <div class="section-label">Perspectivas</div>
  <p class="section-title">Projeções para 2026</p>

  <div class="outlook-grid">
    <div class="outlook-card">
      <div class="outlook-number">+11%</div>
      <div class="outlook-label">Crescimento esperado para o setor em 2026 (ABAC)</div>
      <div class="outlook-desc">A ABAC projeta crescimento de até 11% no total de adesões para 2026, com potencial de novos recordes em participantes ativos. A previsão é mais conservadora do que o resultado realizado em 2025, mas segue indicando um mercado em franca expansão.</div>
    </div>
    <div class="outlook-card">
      <div class="outlook-number">+25%</div>
      <div class="outlook-label">Projeção para imóveis em 2026 — maior crescimento esperado</div>
      <div class="outlook-desc">O segmento imobiliário lidera as projeções. Com juros elevados no crédito tradicional e alta demanda habitacional, o consórcio de imóveis se posiciona como alternativa competitiva — janela estratégica direta para a Ademicon em 2026.</div>
    </div>
  </div>

</div>

<!-- FOOTER -->
<div class="footer">
  <div class="footer-left">
    Fonte: ABAC — Associação Brasileira de Administradoras de Consórcios<br>
    Boletim do Sistema de Consórcios · Janeiro 2026 (dados de Dezembro 2025)<br>
    Elaborado por <strong style="color:#ddd;">Gabriel Francisco</strong> — Grupo Ágape Londrina · Fevereiro de 2026
  </div>
  <div class="footer-right">
    <div class="for-label">Preparado para</div>
    <div class="for-co">ADEMICON</div>
  </div>
</div>

<script>
  Chart.defaults.color = '#888888';
  Chart.defaults.borderColor = 'rgba(0,0,0,0.06)';
  Chart.defaults.font.family = 'DM Sans';

  const RED   = '#c0392b';
  const RED2  = '#e8a09a';
  const GRAY  = '#aaaaaa';
  const GRAY2 = '#cccccc';

  // 1. ADESÕES
  new Chart(document.getElementById('adesoes'), {
    type: 'bar',
    data: {
      labels: ['2021','2022','2023','2024','2025'],
      datasets: [{
        data: [3.05, 3.52, 3.95, 4.49, 5.16],
        backgroundColor: ['#e8cfcd','#d9a49f','#ca7972','#ba4e46', RED],
        borderWidth: 0,
        borderRadius: 3,
      }]
    },
    options: {
      responsive: true, maintainAspectRatio: false,
      plugins: {
        legend: { display: false },
        tooltip: { callbacks: { label: ctx => ` ${ctx.raw.toFixed(2)}M cotas` } }
      },
      scales: {
        y: { grid: { color: 'rgba(0,0,0,0.04)' }, ticks: { callback: v => v+'M' } },
        x: { grid: { display: false } }
      }
    }
  });

  // 2. VOLUME
  new Chart(document.getElementById('volume'), {
    type: 'line',
    data: {
      labels: ['2021','2022','2023','2024','2025'],
      datasets: [{
        data: [190, 252, 310, 378.73, 500.27],
        borderColor: RED,
        backgroundColor: 'rgba(192,57,43,0.07)',
        borderWidth: 2.5,
        pointBackgroundColor: RED,
        pointBorderColor: '#fff',
        pointBorderWidth: 2,
        pointRadius: 5,
        fill: true,
        tension: 0.35
      }]
    },
    options: {
      responsive: true, maintainAspectRatio: false,
      plugins: {
        legend: { display: false },
        tooltip: { callbacks: { label: ctx => ` R$ ${ctx.raw} Bi` } }
      },
      scales: {
        y: { grid: { color: 'rgba(0,0,0,0.04)' }, ticks: { callback: v => 'R$'+v+'Bi' } },
        x: { grid: { display: false } }
      }
    }
  });

  // 3. SEGMENTOS
  new Chart(document.getElementById('segmentos'), {
    type: 'bar',
    data: {
      labels: ['Imóveis','Veículos Leves','Motocicletas','Eletroeletrônicos','Serviços'],
      datasets: [
        {
          label: 'Adesões (%)',
          data: [36.0, 12.0, 8.3, 51.0, 18.0],
          backgroundColor: RED,
          borderRadius: 3, borderWidth: 0,
        },
        {
          label: 'Créditos (%)',
          data: [48.4, 22.0, 15.0, 141.3, 26.1],
          backgroundColor: GRAY2,
          borderRadius: 3, borderWidth: 0,
        }
      ]
    },
    options: {
      responsive: true, maintainAspectRatio: false,
      plugins: {
        legend: { position: 'top', labels: { boxWidth: 12, font: { size: 12 } } },
        tooltip: { callbacks: { label: ctx => ` ${ctx.dataset.label}: +${ctx.raw}%` } }
      },
      scales: {
        y: { grid: { color: 'rgba(0,0,0,0.04)' }, ticks: { callback: v => v+'%' } },
        x: { grid: { display: false } }
      }
    }
  });

  // 4. PARTICIPANTES
  new Chart(document.getElementById('participantes'), {
    type: 'line',
    data: {
      labels: ['2021','2022','2023','2024','2025'],
      datasets: [{
        data: [7.8, 9.1, 10.2, 11.21, 12.76],
        borderColor: GRAY,
        backgroundColor: 'rgba(150,150,150,0.07)',
        borderWidth: 2.5,
        pointBackgroundColor: '#555',
        pointBorderColor: '#fff',
        pointBorderWidth: 2,
        pointRadius: 5,
        fill: true,
        tension: 0.35
      }]
    },
    options: {
      responsive: true, maintainAspectRatio: false,
      plugins: {
        legend: { display: false },
        tooltip: { callbacks: { label: ctx => ` ${ctx.raw.toFixed(2)}M participantes` } }
      },
      scales: {
        y: { grid: { color: 'rgba(0,0,0,0.04)' }, ticks: { callback: v => v+'M' } },
        x: { grid: { display: false } }
      }
    }
  });

  // 5. PROJEÇÃO 2026
  new Chart(document.getElementById('projecao'), {
    type: 'bar',
    data: {
      labels: ['Imóveis','Eletroeletrônicos','Geral','Serviços','Motocicletas','Veículos Leves','V. Pesados'],
      datasets: [{
        data: [25, 20, 11, 10, 7, 6, 0],
        backgroundColor: ctx => {
          const v = ctx.dataset.data[ctx.dataIndex];
          if (v === 0)  return GRAY2;
          if (v >= 15)  return RED;
          return RED2;
        },
        borderRadius: 3, borderWidth: 0,
      }]
    },
    options: {
      indexAxis: 'y',
      responsive: true, maintainAspectRatio: false,
      plugins: {
        legend: { display: false },
        tooltip: { callbacks: { label: ctx => ` Projeção: ${ctx.raw}%` } }
      },
      scales: {
        x: { grid: { color: 'rgba(0,0,0,0.04)' }, ticks: { callback: v => v+'%' } },
        y: { grid: { display: false } }
      }
    }
  });
</script>
</body>
</html>